.gradle/
build/
.intellijPlatform/
# Local Maude checkout used only to regenerate keyword tables
maude-src/
*.tsbuildinfo
.idea/
# Plugin signing key/cert — never commit. Store in a password manager (1Password).
private.pem
chain.crt
*.pem
